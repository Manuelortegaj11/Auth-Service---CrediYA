server:
  port: 8080

spring:
  application:
    name: "AuthService"
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/postgres
    username: root
    password: progresoempresarial11
  h2:
    console:
      enabled: false  # Desactivar H2 ya que usas PostgreSQL

adapters:
  r2dbc:
    host: "localhost"
    port: 5432
    database: "postgres"
    schema: "public"
    username: "root"
    password: "progresoempresarial11"

management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true

cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"